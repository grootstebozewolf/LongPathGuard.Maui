# LongPathGuard.Maui

Prevents long path (MSB3026) errors in .NET MAUI iOS builds from Windows.

Built because nested edge cases are painful.

## Installation
`xml
<PackageReference Include="LongPathGuard.Maui" Version="1.0.0" />
`